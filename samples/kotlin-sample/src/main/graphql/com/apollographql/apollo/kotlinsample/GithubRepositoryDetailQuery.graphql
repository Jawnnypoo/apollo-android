query GithubRepositoryDetail(
  $name: String!
  $pullRequestStates: [PullRequestState!]!
) {
  viewer {
    repository(name: $name) {
      # parent {
      ...RepositoryDetail
      # }
    }
  }
}
